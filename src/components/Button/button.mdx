# Button 组件

使用方式如下所示：

import { Playground, Props } from 'docz'
import Button from './button'
import "../../index.scss"

# Properties

<Props of={Button} />

## Basic usage

<Playground>
	<Button>Normal Mode</Button>
	<Button disable={true}>Normal Mode</Button>
	<br/>
	<Button mode="calm">Calm Mode</Button>
	<Button mode="calm" disable={true}>Calm Mode</Button>
</Playground>